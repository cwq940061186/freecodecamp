//算法题目：翻转字符串
//思路：先把字符串转化成数组，再借助数组的reverse方法翻转数组顺序，最后把数组转化成字符串。

//可能用到的方法：
//Global String Object
//String.split()
//Array.reverse()
//Array.join()

//代码：
function reverseString(str) {
  
  return str.split("").reverse().join("");
  //把数值按“”分割成数组，并赋值给array。把array数组翻转。把数组按照“”合并成一个，并赋值给str。
  
}

//测试:
//reverseString("hello") 应该返回 "olleh".
//reverseString("Howdy") 应该返回 "ydwoH".
//reverseString("Greetings from Earth") 应该返回 "htraE morf sgniteerG".
